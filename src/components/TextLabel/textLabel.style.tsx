import styled from "@emotion/native";

export const CommonLabel = styled.Text({
  color: "#46494c",
});

export const RowLabel = styled.Text({
  flex: 1,
  padding: 8,
  borderWidth: 1,
  borderColor: "#46494c",
});

export const HeaderLabel = styled.Text({
  color: "#1985a1",
  alignSelf: "center",
  fontSize: 25,
});

export const CellLabel = styled.Text({
  color: "#4c5c68",
  paddingEnd: 0,
  paddingStart: 10,
  fontSize: 16,
  flex: 1,
});
